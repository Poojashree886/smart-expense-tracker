# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/expense_tracker_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Shrutipooja@188
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8081

# JWT Configuration (we'll add more later)
jwt.secret=mySecretKeyForJWTGeneration2024SecureRandomText
jwt.expiration=86400000

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=poojashree.v886@gmail.com
spring.mail.password=gktd ozwt jgdn wlpk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email Settings
app.email.from=poojashree.v886@gmail.com
app.email.alerts-enabled=true
app.email.budget-threshold=80

# Disable HTTP/2
server.http2.enabled=false

# Disable compression
server.compression.enabled=false

# Increase timeout settings
server.connection-timeout=60000
server.tomcat.connection-timeout=60000
server.tomcat.max-keep-alive-requests=1

# Enable debug logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.expensetrackers=DEBUG
logging.level.org.springframework.boot.web=DEBUG